<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Login</title>
    <link rel="stylesheet" href="login.css" />
  </head>
  <body>
    <p class="tip">Sign In with Team Password</p>
    <div class="cont">
      <div class="form sign-in">
        <h2>Welcome back,</h2>
        <form action="/Login" method="POST">
            <label>
                <span>Email</span>
                <input type="text" required name="Team_name" placeholder="Team Name.."/>
              </label>
              <label>
                <span>Password</span>
                <input type="password" required name="Team_Secret" placeholder="password"/>
              </label>
              <button type="submit" class="submit">Sign In</button>
        </form>
        <button type="button" class="fb-btn">
          Connect with <span><a href="/auth/github">Github</a>  </span>
        </button>
        <button type="button" class="fb-btn">
            Connect with <span><a href="/auth/google">Google</a> </span>
          </button>
      </div>
      <style>
      a{
        text-decoration: none !important  ;
      }
      </style>
      <div class="sub-cont">
        <div class="img">
          <div class="img__text m--up">
            <h2>Login here?</h2>
            If you already has an account, just sign in. We've missed you!
          </div>
          <div class="img__text m--in">
            <h2>One of us?</h2>
            <p>
              If you already has an account, just sign in. We've missed you!
            </p>
          </div>
          <!-- <div class="img__btn">
            <span class="m--up">Sign Up</span>
            <span class="m--in">Sign In</span>
          </div> -->
        </div>
       
        </div>
      </div>
    </div>

    </a>
  </body>
  <script>
    const input = document.querySelector('input');
    const form = document.querySelector('form');
    form.addEventListener('submit',(e)=>{
     if( input.value.length == 0){
       alert('the fields are required')
     }
    })
  </script>
</html>
